package config

// Database means the name of database
const Database = "nfly"

const (
	DBHost = "127.0.0.1"
	DBPort = "3306"
)

const (
	DBUser = ""
	DBPwd  = ""
)

// DBPath means the path where the sqlite db file are.
const DBPath = "./nfly.db"
